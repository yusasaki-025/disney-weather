# OpenWeather 連携を有効にする場合のみ (Phase 2)
OPENWEATHER_API_KEY=
# Cloudflare Workers プロキシ URL (Workers にキーを置く場合)
OPENWEATHER_PROXY_URL=

# 環境省 WBGT プロキシ URL (workers/wbgt-proxy.js のデプロイ先、§0.10)
# 設定すると WBGT が簡易計算から環境省実値に切り替わる (提供期間 概ね 4-10 月)
WBGT_PROXY_URL=
