---
name: project_disney_weather
description: ディズニー行く日決め天気比較ダッシュボード (個人) の進捗と残セットアップ
metadata: 
  node_type: memory
  type: project
  originSessionId: 400e7948-ec8c-436e-b919-94faabc5966a
---

TDL ･ TDS に行く日を決める天気比較ダッシュボード。単一 HTML artifact (Cowork 向け)。

**製品名 : 「マイハマびより」** (§0.11、2026-05-30 改称、旧「Disney 行く日きめるダッシュボード」)。Disney 商標回避。repo 名 ・ 公開 URL ・ 内部識別子は `disney-weather` のまま維持 (CF Pages 再連携回避)。

場所 : `~/claude/personal/disney-weather` (個人プロジェクト)。公開 : https://disney-weather.pages.dev (CF Pages、main push で自動デプロイ)。

**置き場所の判断** : 仕様書は `komorebi-tools/disney-weather` を予定としていたが、内容は個人利用のため personal に配置 ( 2026-05-30 Yuka 確認済み )。業務組織には置かない。GitHub に上げるなら個人アカウント yusasaki-025。[[feedback_no_work_personal_mix]]

**状態 ( 2026-06-04 ・ 最新 )** : **§0.40〜§0.69 まで本番反映済** (公開中、push で自動デプロイ)。Vitest 186 緑 / ESLint 0 / build OK。

**【最重要】スコアリングモデルの根本変更 (§0.65-§0.69, 2026-06-04)** ※後続セッション必読 :
- **§0.65** WBGT 含む数値の表示精度統一 : 風速 ・ 雨量 mm/h ・ WBGT は小数 1 桁 (`fmtNum(v,1)`)、雨確率 % ・ 気温 ° は整数。`src/utils/metrics.js` に `round1`/`showWindowOrMax` 集約。
- **§0.66** 日スコア = **時間帯サブスコアの加重平均** (BANDS weight 朝1.5/昼2.0/夜1.0 ÷4.5、`weightedBandTotal`)。旧「全要素加重平均 rawScore」から転換。`bandFloorCap` (時間帯 NG→日≤59 / FAIR→日≤74) 追加。§0.42.4 クランプ (時間帯≦日) は**撤廃**。evaluateDay 戻り値に `base`(=bandAvg)/`floorCap` 追加、`capped = score < base`。
- **§0.68.D → §0.74 で改訂** : §0.68.D は日バッジ = 各時間帯バッジの最悪 (worstBandBadge) にしたが、ショー時刻外 (朝/夜) のピークで「表示数値 8.4m/s なのに中止リスク高」乖離が出たため **§0.74 で撤廃**。**現行 : 日バッジ = ショー時刻 showWindow 由来** (`windBadge(showWindowOrMax(metrics,'gust'))` 等) = カード表示数値と同ソースで必ず一致。worstBandBadge ・ bandSubscore の band 別 badges は撤廃済。
- **§0.69** 通常バッジ (cancel-lv0=緑) は windBadge が「通常」を返すため全セル常時表示 (§0.74 後も維持)。
- ※ §0.74 の留意点 : 日スコアは §0.66 の band 加重平均 (ピーク + floorCap) のままなので、構造上ショー時刻外 band ピークでスコアだけ下がりバッジ (showWindow) と乖離する余地が残る (Yuka は数値↔バッジ整合を優先)。
- **§0.72** 警告上限は要素別 `warnElementCap` (風単独 GOOD89 / 雨 or 熱単独 OK74 / 雨+熱 FAIR59 / 3つ FAIR59 ・ 風 < 雨 ・ 熱)。
- **§0.75** 詳細パネルのショー行に個別風閾値「基準 : 風バ X 〜 ・ 中止 Y 〜」併記 (thresholdForShow ・ isDefault で一般基準注釈 ・ 屋内は非表示)。
- **§0.76** ショー priority 再設定 : **ハーモニー high→medium (通年) ・ スカイ low→high (期間限定花火)**。priority=high が showWindow (バッジ/スコア判定窓) を決める。TDL high = ジュビレーション 15:00 + スカイ 20:30 (ハーモニー 13:00 は judge 対象外 ・ ショー個別 #18 では表示)。schedule/*.json は**行単位置換で整形保持** (json.dumps 全体再整形は巨大 diff になり NG)。
- **§0.78** ★スコア 3 層構造に確定 : (1) 基準 = band 加重平均だが **activeBands** (showWindow に重なる band のみ ・ §0.78.1) で算出 → スコアとバッジ (§0.74) を同一時間範囲に。(2) 上限 cap = §0.55/§0.72/§0.16/bandFloorCap。(3) **下限 floor = warnFloor** (§0.78.2 ・ 警告0=75 / 風単独=60 / 雨 or 熱単独 ・ 警告2=40 / 警告3 ・ danger=20 / critical=0)。最終 = `max(floor, min(base, caps))`。これで「警告なし=最低 GOOD」担保 + 寒さ/UV 下げ過ぎ防止 + 6/9 vs 6/10 逆転解消。分布が BEST1/GOOD4/OK4/FAIR3/NG3 と §0.55 目標に整合 (floor 導入前は GOOD0/FAIR9 偏重)。
- **§0.79** カード上の風見出しは「突風」(showWindowOrMax gust = ピーク値 ・ METRIC_HEAD/METRIC_LABEL/thead catHead)。
- **§0.81** PC 詳細パネル背景も白統一 (.detail-row td #fff)。ショー行は文字色 3 階層 (現状データ=濃黒 --text / 「現状」「基準」ラベル=中グレー .sr-lead / 基準行=薄グレー --text-mute) + ｜ 区切り + 行間 8px padding。時間帯スコアの「← 最重視」テキストは撤去し昼セルはゴールド枠 + 淡ゴールド背景 (#fffaf0) + 太字で強調 (.ss-row-main)。
- **§0.82** スマホ雨セル 2 行折り返し (文字大時) の行間タイト化 (.cell-rain .cat-val row-gap 2px) + 風/雨セルを align-self:center で垂直中心揃え。
- **§0.80** 天気セルにも 5 段階バッジ (快適/ふつう/注意/警告/悪天候 ・ `src/data/weatherBadge.js` ・ weatherText から判定 ・ 降水を晴/曇より優先 ・ 表示用でスコア不影響 ・ 気象庁/OM 両ソース ・ .weather-badge/.wb-*)。
- **§0.77** daySummary は **3 段階** (全ショー開催予定 / 開催予定(○○可能性) / 中止の可能性(複数) / 中止の可能性が高い(キャン) / 中止濃厚(中止)) + **priority high ショー名併記**。scoreReason ・ daySummary とも「原則 / ことが多い / 見込み / 大きな支障」等の曖昧表現を全廃。
- **§0.68.H.b** cold/UV も bandSubscore に組込 (日次値 ・ 全時間帯一律)。バッジは中止リスク (風/雨/熱) 専用で cold/UV はバッジ無し=快適性スコアのみに反映。
- **§0.72** 警告バッジの上限を**要素別重み付け** (`warnElementCap`、旧 §0.55.5 個数ベース `warnCountCap` を置換) : 風単独=GOOD(89) / 雨 or 熱単独=OK(74) / 雨+熱=FAIR(59) / 3つ=FAIR(59)。風 < 雨 ・ 熱 (Yuka 知見)。
- **§0.71** scoreReason は**直接理由文** (`getScoreReason(eval)` = 警告バッジの有無 ・ 数。「警告 3つ (風バ+雨バ+熱バ)」「風バ可能性あり (8.2m/s) ・ 風は通常開催されることが多い」「雨と熱の両方が注意」)。加重平均等の中間計算は UI 非表示。時間帯別 ss-rows は「時間帯別 (参考)」見出しで下に。
- **§0.70.2** 詳細パネル score-pill アイコンは `.score-head .score-pill .material-symbols-rounded { color: inherit }` で信号機色を継承 (§0.68.A で h4 内移動した際の青色化退化を修正)。
- **§0.68.A-H + §0.69 = 監査 CONSISTENCY-AUDIT.md (§0.67) の修正実装**。docs/CONSISTENCY-AUDIT.md と docs/DESIGN-AUDIT.md が監査一次源。
- 14日分布は BEST/GOOD が痩せ FAIR 偏重 (band ピーク基準 + バッジキャップ) だが Yuka 判断で**放置** (weather 依存)。
- 残: §0.68.H.a の period 機構は実装済だが period 値未設定 (公演終了日を Yuka が手動入力する運用 ・ §0.39.7)。§0.68.C 保留の小物 dead CSS (.row-main.is-ng 等 ・ 壊れた機能の可能性)。L-2 以外の監査中項目は一巡完了。

以下は §0.40-§0.51 時点の履歴 (一次情報は docs/CHANGES.md §0.x)。

**実装到達点 (§0.40〜§0.51、すべて本番)** :
- §0.40-0.46 UI 改善 第3〜7弾 : ショー欄完全並列 (priority 装飾全廃 ・ タグのみ区別) ・ 時刻先頭 ・ 複数公演2行 ・ toggle 撤廃常時展開 ・ タグ色 (DPA 薄水/抽選 薄紫/期間限定 薄ピンク §0.45.1) ・ スマホ雨セル均等3分割中央 (§0.46.10) ・ 警報を「この日の概要」集約 ・ 時間帯スコアに日全体 (カード同デザイン) ・ グラフ降水/風速2分割 ・ 持ち物天気依存のみ ・ 屋内ショー weatherless ・ スカイ時刻順 ・ レストラン末尾 ・ スマホフォント底上げ。
- **§0.46.12 文字サイズ機能** : 小(現状)/中(1.15×)/大(1.3×)、`html[data-font-size]` + body `zoom` (px ベースで rem スケール不可のため zoom 採用)、localStorage 永続、head インラインで描画前復元。ヘッダーは §0.51.2 でカウンター zoom (`.app-header` を 1/1.15・1/1.3) し固定サイズ。PC=ヘッダー / スマホ=絞り込み下 `.mobile-action-row` (§0.49) の2箇所コントロール相互同期。
- **§0.47 スコア過剰減点バグ修正** : 日全体風バッジを一般ショー基準 `DAY_WIND_THRESHOLD={windBa:8,windCancel:11}` に (ハーモニー6m/s特例は詳細のみ) ・ 風/雨減点緩和 ・ SEVERITY_CAP 緩和 (風バ70/中止リスク40/中止20)。14日で別日/微妙のみ→ベスト/OK/微妙/別日バランス化。
- **§0.48 雨単位混在 + 霧雨過剰判定修正** : `metrics.precipMaxHourly` 新設、雨セル/バッジ/減点を**時間最大 mm/h ベース** (<1通常/1-3雨バ/3-5雨キャン/≥5中止) に。日合計 precipSum は中止判定に不使用。霧雨 (weatherText「霧雨」) は雨バ上限固定 + 概要注釈。6/10「13mm ほぼ中止別日」→「0.7mm/h 通常ベスト90」。
- **§0.50 警報カード重複削除** : `renderTodayWarning` 廃止、警報は概要のみ (「カード上維持」は Cowork 仕様文ミス訂正)。
- **§0.51 文字大崩れ対策** : 更新ボタン「5分前」のみ (aria/title維持) ・ ヘッダー固定 (counter-zoom) ・ 風速小数1桁 (7.6/8.2 m/s、8m/s境界の表示矛盾解消、showRisk.meanAt(field,decimals))。
- #23 (§0.39.5 予報ソース重み付け MAE) は実装済だが accuracy-log が少数のため等重みフォールバック中 (5日蓄積で発効、ダッシュボードに重み可視化)。

**進行ルール (厳守)** : 1 Issue = 1 PR (混ぜない) ・ Edit 前 Read 必須 ・ commit 前に Vitest/build/ESLint の exit code 確認 ・ commit 直後 `git show HEAD:file|grep` で着地確認 ・ 並行 cron 衝突に rebase ガード (fetch→behind>0なら stash+rebase+pop→push→L=R確認) ・ DevTools (chrome-devtools MCP) で PC1280+スマホ375 実描画確認 ・ Phase 0 調査で「実は動いている/不要」を見極め過剰実装回避。

**残宿題** : #24 (§0.39.6 TDL/TDS パーク別補正) ・ #29 (§0.39.11 Lighthouse) は **wontfix** (Yuka 判断) ・ #23 重み付けはデータ蓄積待ち ・ docs/CHANGES.md §0.x が逐次の一次仕様源。

**改訂仕様で反映済み** : 日付順デフォルト ・ TOP3 下部 ・ 記号刷新 (● ▲ ■ 廃止 → 記号+数値+色+Material アイコン) ・ 観測地点 config(location.js) ・ データ鮮度ラベル ・ disclaimer ・ ダークモード(§6.6) ・ ヘルプ用語集(§3.16) ・ 雨雲レーダー(§3.18、CSP で iframe 不可→直リンクカード) ・ 印刷モード(§3.17) ・ 60 秒自動更新(§6.7)。

**改訂仕様で未実装 ( ユーザー判断で見送り )** : 自動通知 scheduled task(§3.19) ・ PWA(§6.8) ・ テスト基盤 msw+Playwright(§7.4)。Phase 2 = TDR 公式スケジュール取得(§3.10/§3.20、個人利用なので規約面は許容と確認済み) ・ 混雑(§3.21) ・ AI サマリ(§3.22) ・ 投票(§3.23) ・ 的中追跡(§3.12) ・ 信頼度補正(§5.4)。

**§0.6 UI 改善 (実装済み)** : サブスコアにラベル必須 ＋ 昼を枠/太字/数値で強調 ・ 気温セル暖寒色(src/utils/tempColor.js) ・ 狭幅ハンバーガー(src/ui/menu.js) ・ 鮮度をステータスバー(src/ui/statusBar.js)に集約しセル鮮度は廃止。

**GitHub** : 既存リポは `yusasaki-025/disney-weather` (gh 認証= yusasaki-025)。ユーザー指定の `yukasasaki` は別ユーザーアカウントで現認証では作成不可 → ユーザー判断で yusasaki-025 を **public 化**して代用。main + feature ブランチ push 済み。GitHub Actions は spending budget 100% 到達で起動しない (6/1 09:00 JST に無料枠リセットだが budget 据置だと再到達、要 budget 引上 or public)。

**§0.4-§0.7 + §0.6.5-§0.6.10 (PR #1)** : feature ブランチ `feat/ui-text-labels-weather-icons` で実装し PR https://github.com/yusasaki-025/disney-weather/pull/1 作成済み (未マージ)。内容 = スコア記号◎○△×全廃しテキストピル化(src/score/scoring.js の SYMBOLS は {key,label,color}) ・ 天気アイコン(src/utils/weatherIcon.js) ・ 詳細2カラム ・ 行chevron ・ ショー実在公演名(showSchedule.js は {name,time,priority,type} スキーマ) ・ TDRトーンのデザイントークン+Noto Serif/Sans JP+Inter ・ QR廃止しURLコピー ・ runtime.js(isCowork)で公開版は個人連携非表示。Vitest 63緑。
**[[feedback_no_accent_bars]]** : §0.6.10 で一旦入れたセクション見出し左ゴールド縦バーは、§0.6.11 (commit 3f9c2a1, main 直コミット) で**削除済み** → ブルー見出し ＋ ピンク下罫 ＋ margin-top で区切る形に。no-accent-bars 方針を最終的に優先。PR #1 はマージ済み。
**docs/SPEC.md ・ docs/CHANGES.md** がリポジトリ内の一次情報 (Yuka 配置)。CHANGES に §0.x で逐次 UI 改修指示が追記される運用。

**UI 改修の到達点 (origin/main da39356, 2026-05-30)** : テーブル 6 列 (日付+スコアピル統合 §0.23 / 風 / 雨 / 熱 / 気象庁 / Open-Meteo) ・ スマホ <768px はカード化 (§0.22 grid-template-areas + data-label) ・ 決定/NG ボタン廃止 (§0.21) ・ スコアはテキストラベル+評価アイコン (ベスト=star/OK=done/微妙=warning/別日=block) ・ ヘッダーは [更新・X分前][ヘルプ] のみ (print/theme/menu 廃止 §0.17) ・ ダークモード廃止で常時ライト ・ フィルター塗りつぶしトグル ・ おすすめ日のみ ・ バッジ severity でスコア上限キャップ (§0.16) ・ 算定窓は平均ベース (§0.13.2)。
**注意** : Cowork host loop の別セッションが同 repo に favicon/OG 等 `public/` を生成することがある (untracked)。コミット時は `git add src/` 等で対象を絞り、public/ を巻き込まない。
**反省** : 約物 (全角中黒/丸括弧) を含む行は Edit の old_string が一致しにくく silent fail しがち。grep で実体確認 → 失敗時は python 置換か Write 全置換に切替。長セッションでは Read/cat/スクショの表示が壊れることがある (書き込み自体は lint/test/build の exit code と chrome-devtools evaluate_script の JSON で健全性を担保できる)。CSS の大ブロック改修は表示非依存の cat >> 末尾追記 (cascade 後勝ち) が安全。

**Phase 2 着手済み (origin/main ce0f4d2, 2026-05-30)** :
- §0.8 日別ショースケジュール (PR #2 マージ済) : scripts/fetch-schedule.mjs (Playwright, UA明示/3s間隔/月1想定) → src/data/schedule/YYYY-MM.json。showSchedule.js の getDaySchedule(date,park) が official/fallback を返し、scoring/chart/table に date 伝播で日別変動。詳細パネルに「公式取得済/典型値で代替」バッジ。**公式は WAF で bot 全ブロック (curl/WebFetch 応答ゼロ・robots.txt も不可)** → 実スクレイピングは Yuka がローカルで `npx playwright install chromium` 後に実行する前提。2026-06.json は手動プレースホルダ (要実取得置換)。playwright は devDep に追加し lock 同期済 (PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm install)。
- §0.24 スマホカード再設計 : 日付+スコア横並び/風雨熱3列/気象庁OM2列/タップで詳細ヒント/余白詰め。--space-*,--fs-* トークン追加。**§0.24 の inline-block+width:% は実機 iPhone で崩れ**、§0.25 (main 8389b43) で **grid+grid-template-areas (date/wind rain/heat heat/jma om/more) に修正**。検証は必ず DevTools 375x812 エミュレーション + getBoundingClientRect 数値 (ウィンドウ resize の目視 NG)。行の td は 6 個 (date-score/wind/rain/heat/jma/openmeteo、chevron td 無し)。
- **§0.26 ショー表示集約 (main 4d8a290)** : 詳細パネルのショー一覧を name 集約 (times を " / " 連結で 1 行) ・ 内部用語「(メイン算定窓)(補助)(参考)」削除 ・ priority を CSS class (.priority-high 太字/-medium 通常/-low グレー --text-mute) で視覚区別。DOM = ul.show-list > li.show-item > span.show-name/.show-times/.show-tags。park タブ切替は [data-park-shows] 属性ベースなので class 変更 (div.show-times→ul.show-list) は安全。
- **git 履歴再編に注意 (2026-05-30)** : 並行 Cowork セッション運用中、push 範囲が予想とずれ commit hash が変わる (7ea041a→消失だが中身は HEAD 保全) ことがあった。pre-commit auto-stash か rebase 由来。**対処 : commit hash でなく "中身が HEAD/remote tree にあるか" を git cat-file -e / git show HEAD:file | grep で機械確認。working tree porcelain クリーン + LOCAL=REMOTE を最終確認すれば欠落なし**。[[feedback_pre_commit_race]] [[feedback_parallel_session_isolation]]
- **WBGT 環境省実値 稼働中 (main 1cc301b, 2026-05-30)** : wbgt-proxy Worker を Cloudflare (pi.pi.pi.025) にデプロイ = `https://wbgt-proxy.pi-pi-pi-025.workers.dev`。CONFIG.wbgtProxyUrl 設定済みで本番 env-jp 実値 (詳細セル title「WBGT 環境省取得値」)。環境省 CSV 実形式は YYYYMMDDHH (10桁ヘッダー) / 値×10 / 空欄=期間外 (§0.10 推測 YYYY/MM/DD は誤り、parseStamp で両対応)。提供は概ね4-10月、期間外/失敗は簡易計算フォールバック。船橋44132・3時間毎。再デプロイ `cd workers && npx wrangler deploy --config wrangler.toml`。wrangler は OAuth が一度 Invalid token (9109) になった → 必ず `wrangler whoami` で検証後 deploy、壊れたら logout で config クリア→再 login。
- **§0.28 運営状況蓄積基盤 (main a7f2e09)** : scripts/fetch-operation.mjs (当日中止/変更/早閉めを operation.html+calendar からパース → src/data/operation-log/YYYY-MM-DD.json)。operationLog.js の getDayOperation/latestOperation を import.meta.glob で取込。detailPanel に「当日中止情報」セクション (取得済の日のみ、block赤/warning橙/schedule、Material Icons で絵文字回避)。**Akamai で headless 不可 → 実取得は Cowork Chrome MCP 経由**。operation-log/ は .gitkeep のみ (偽中止情報を公開しないため空で commit)。第4弾=的中追跡の基礎。**重大バグ (fix 753b7b1, 2026-05-31)** : §0.28 で operationHtml が latestOperation() を呼ぶのに table.js に import 漏れ → 行クリックの度 ReferenceError で詳細パネルが全く開かなかった (全環境再現)。検証時 operation-log が空でセクションが出ない=正常と誤認し見逃した。**教訓 : 新 import を使う関数を追加したら必ず実際にその経路 (タップ等) を動かして console error 確認。空データで「出ない」のは「壊れて出ない」と区別できない**。さらに **ESLint no-undef が latestOperation 未定義を検出できていない** (eslint 設定見直し要、別タスク)。
- **§0.31 スマホ中央寄せ (main 753b7b1)** : スマホカードの風/雨/熱を text-align:center で気象庁/Open-Meteo と統一。(注: コミットコメント上 §0.31 を中央寄せに誤用したが、CHANGES.md の正式 §0.31 は下記の中止確率表示)
- **§0.31 過去中止率表示 (main b06c8f7, Phase 3 第1弾)** : cancelHistoryLoader.js (cancel-history/*.json を glob、**ショー名は全角中黒「・」vs 半角「･」で不一致 → normalizeShowName で中黒/感嘆符/空白/波ダッシュ除去して突合**、rec=0 ゴミ抽出除外) + cancelProbability.js (getCancelProbability: 予報 max 風速 ±2m/s の過去事例で cancel+partial-cancel 率、全体<20 or 同風速<5 は null)。table.js 各ショー行に「予報 Nm/s → 過去 X件中 Y件中止 (Z%)」併記 (≧50%赤/30-49%黄/<30%グレー)。実データ照合: ハーモニー 9m/s→6%・13m/s→60%・4m/s→0%。**§0.30 パーサは TDS で列ずれゴミ (時刻/数値をショー名と誤認、全て rec=0) を生成するが確率計算には無害**。
- **§0.32 予報精度ダッシュボード (main 7ec8800+補修)** : 独立ページ /accuracy.html。**vite-plugin-singlefile は複数 input 非対応 → vite.accuracy.config.js を別 config 化し build 2 パス (`vite build && vite build --config vite.accuracy.config.js`、2 パス目 emptyOutDir:false/publicDir:false)**。accuracyLogLoader.js (RMS/bias/timeSeries/notableExamples) + accuracyDashboard.js (Chart.js 時系列+RMS/バイアス表+的中/外し例、7日未満は蓄積中)。index フッターに「予報精度を見る」。accuracy-log は空 {} で公開。8日シードで実 DOM 確認。
- **ESLint 完全化 (main 3f59f2e で最終確定)** : eslint.config.js に scripts/**/*.mjs (node+browser globals、page.evaluate 内 document 用)、tests に vitest globals 手動 (describe/it/expect/vi、globals パッケージに vitest 無し)、src に Chart (CDN UMD)。全角空白 U+3000 除去。**以後 `npx eslint .` 0 error → import 漏れ ・ undefined は必ず lint で止まる**。
- **§0.36 UI 微調整 11点 (main 3f59f2e, Phase 3)** : バッジ短縮 (風バ可能性あり→風バ/中止リスク高→中止リスク/ほぼ中止→中止/雨キャン濃厚→雨キャン 等、badgeSeverity と help.js も同期、scoring.test.js 期待値更新)、weatherText.js (くもり→曇り・時間帯語前に読点)、extremeWarning.js (gust≧20 or 雨≧30mm/h で「(要確認)」)、rainSub (pop% mm/h・0%非表示・日合計 title)、freshness 60分以上は HH:MM、おすすめ日のみ先頭+star、副題スマホ短縮 (subtitle-full/short CSS)、セル高さ詰め・カード gap・chevron 視認性 (CSS)。
- **§0.37 UI改善12項目 (Phase 3、10/12 完了, main c1b9ccf)** : 本番反映済=#1 バッジPC長文/スマホ短縮 ・ #2 熱4階層(暑さ注意→熱バmerge) ・ #3 ショーカード化(surface-2/high=warn-bg) ・ #5 詳細色統一 ・ #6 二重シャドウ解消 ・ #7 雨セル拡幅(grid wind1/rain3/heat2) ・ #9 曜日short(平日のみ→平日) ・ #10 scoreReason.js(スコア下に理由, 5テスト) ・ #11 chart指標別色(降水青/風ティール/気温赤/体感橙+ソース線種) ・ #12 TOP3余白。**未完=#4 PCスコア列(未着手, table.js cell-date/cell-score分離+mobile grid 4ブロック横断要)・#8 スマホヘッダー再構成(壊れてrevert済=mobile更新ボタンEdit失敗で消失→ee2730bでrevert)**。Vitest 87緑/ESLint 0/build OK。各1コミット分割。**重大教訓: 並行プロセス(linter/別Coworkセッション)が src/index.html ・ components.js の編集を繰り返し巻き戻す(#9 spanが2回消失, §0.37-1/2 でcomponents badge-long消失)。対策=commit直後に必ず `git show HEAD:file|grep` で着地確認, 消えてたら再投入。加えて長セッションでBashとReadの出力描画が断続的に全く見えなくなる事象あり(git/buildのexit codeとMCP evaluate_scriptのJSONで健全性担保)。**

- **§0.38 UI改善 第2弾 (全21項目中 20完了, main 4af8946, 2026-05-31)** : #1 持ち物拡充(outfit.js metrics入力, tests)・#2 ヘルプmodal z-index 1000>thead100 + 背面scrollロック・#3 DPA用語集・#4 時間帯スコア時刻範囲(BANDS.hours から 9-12/12-16/18-21時導出)+昼最重視・#6 雨セル単位.unit・#7 気象庁cloud/OM partly_cloudy_day見出しアイコン・#8 熱WBGT info tooltip+用語集・#9 キャン用語整理・#10/#15 (要確認)button化+score-row flex+FAQ・#11 中止確率［過去中止X%(n/m件)］・#12 グラフ時刻ラベル→▼マーカー・#13 PWAバナーstatic+footer直前・#14 スマホborder-top削除・#16 曜日色(土青/日祝赤 dt.weekdayIndex)・#17 ショー名accent廃止・#18 抽選用語・#19 レストラン予約必須・#20 確定情報・#21 ショー行詳細をdetails/summary折りたたみ。**未完1= #5 気象庁pop週間予報フォールバック(jma.js 165行・120000.json は週間pop含むが現normalizeは時間別popのみ拾う・実APIレスポンス構造調査+実データ検証必須・本番コアdata source破壊リスク高→要相談 or ツール安定時)**。Vitest 93緑/ESLint 0/build OK。**#21注: 仕様前提「ショー行に風8m/s熱WBGT27常時表示」は現コードに無く(per-show風熱未実装), 折りたたみ対象は過去中止率にした(要相談)。** 教訓: このセッションは Edit の old_string が高頻度で不一致になり(modal CSS 2回・help-content WBGT・util.test)、commit が部分適用→push後に実測で発覚を繰り返した。**対策: Edit直後に commit せず必ず git show/preview実測で着地確認してから commit、test fail 中は commit しない(各commit前に Vitest Tests 行必読)。**

- **GitHub Issues 運用化 + §0.37 carry-over 2件完了 (2026-05-31, main 7f98645)** : docs/CODE-REQUEST-NEXT.md を元に Issue 15件 (#15-18=carry-over+§0.38残, #19-29=§0.39第3弾)・ラベル6種 (phase-3/ui/data/ops/a11y-perf/carry-over)・マイルストーン「§0.39 機能拡張 第3弾」を gh CLI で作成、README にロードマップ節追記。**#15 §0.37.4 PC スコア列復活** (PC 7列, スマホはカード統合維持。実装=モバイル grid 不変+独立 td.cell-score-pc 追加+カード内スコア.card-score を CSS トグル, colspan 5→6) と **#16 §0.37.8 スマホヘッダー再構成** (ヘルプ icon only+フィルター下に全幅 #btn-refresh-mobile, 前回 ee2730b revert のリベンジ成功) を実装・実測・close。Vitest 93緑/ESLint 0。**重要: §0.40 はユーザー指示があったが docs/CHANGES.md に存在せず (確認済 0 occurrences, 最終見出しは §0.39)。さらに §0.39.1-.11 は全て「(仕様 TBD)」プレースホルダーで実装可能な詳細仕様なし。残る実仕様つき Issue は #17 §0.38.5 気象庁pop週間予報補完 (本番 data source 改修・実データ検証必須・要相談) と #18 §0.38.21+ 熱per-show (データ層追加・要相談) のみ → solo 着手せず Yuka 帰宅後に仕様確認 or 一緒に。捏造もリスキー data-source solo 改修もしない判断で区切った。** 教訓再掲: Issue 化で最初依頼文 (15件/phase-3等/§0.39 ms) を読み違え A-1〜E-2 12件+独自ラベルを作成→全 close+削除して作り直し。依頼文は逐語確認してから gh 実行。出力 glitch 多発セッションは node JSON/exit code/MCP eval で確認。

- **§0.40 UI改善 第3弾 (全8項目中 6完了, main 49b6e84, 2026-05-31)** : §0.40 は docs/CHANGES.md L4132 に実在する明確仕様 (前回「存在しない」と誤判定したのは grep ミス、お詫び済)。Issue #30-37 + マイルストーン「§0.40 UI 改善 第3弾」作成。完了= #32 (.3 雨セル cat-val に justify-content:center 明示, 実測 wind/rain/heat offset0)・#33 (.4 通常タグ=緑 --ok-green #2e7d32, lv0)・#34 (.5 ショー同形式化: .show-item の surface-2 カード背景撤廃→下罫線のみ左寄せ, priority-high の warn-bg/太字廃止, DPA/抽選/期間限定を独立 .show-tag バッジ化 tag-dpa=accent/tag-chusen=紫/tag-season=ok-green)・#35 (.6 ショー toggle 文言「開催予想 ▶/▼」)・#36 (.7 雨雲レーダーを .detail-section+h4+cloud_queue に統一)・#37 (.8 見出し「降水確率 ･ 風速 (時系列)」順序入替)。**未完2 (大・構造改修, 相互依存)= #30 (.1 スマホ詳細パネルをカード内に格納: 現状 detail-row は別 <tr>, max-height transition でカード内 toggle 化要・モバイル grid 領域=§0.37.8 revert 事故と同領域)・#31 (.2 この日の概要見出し: #30 のカード内 toggle 構造に依存, scoreReason+(要確認)理由を toggle 内へ移動)**。両方とも実機確認必須の高リスク構造改修のため、お出かけ中・監視なし・出力glitch多発の状況では着手せず次セッション (実機確認可能時) に持越し。Vitest 93緑/ESLint 0/build OK。各1コミット分割。

- **§0.41 待ち + スクショ指摘8点の事前調査 (2026-05-31, main 6777f9c)** : Yuka さんが §0.41 依頼文を Cowork で整理中。スクショ指摘8点をコード照合した結果: **#1「降水量」誤実装 ・ #4「Liveアクト」タグ ・ #7「ご覧」途切れ文 は現行コードに grep ゼロ = 存在しない** (本番キャッシュ or デプロイ反映前の古い画面の可能性大、ハードリロード後の実機再確認を §0.41 で要依頼)。Y軸は「降水確率 %」で正しい。schedule tags は プレミアアクセス/エントリー受付/予約必須 のみ (Liveアクト 無し)。**#8 ショー名 priority-high の太字 (font-weight:600) は §0.40.5 の Edit 漏れで実在 → 6777f9c で 400 に修正済み**。残る要対応 (§0.41 依頼文待ち): #2 ショー時刻5連の冗長簡潔化 (現 g.times.join(" / "))・#3 .show-item の下罫線が帯に見える・#5 toggle 文言「タップで詳細/閉じる」整理・#6 詳細展開の時間帯スコア「78」がラベル無しで何のスコアか不明。これらは簡潔化の形式に解釈余地があるため依頼文待ちで先行せず。未着手の §0.40.1/.2 (#30/#31 スマホ詳細カード内格納) も継続保留。

- **§0.41 UI改善 第4弾 (10/12 完了, main 4b8e1f7, 2026-05-31)** : §0.41 は CHANGES.md に未記載で docs/CODE-REQUEST-NEXT.md の12タイトルのみが仕様源。**重大ミス: 最初 §0.41 仕様を読んだのに前回スクショ推測8項目とごっちゃにし、別中身で Issue #38-49 + 実装してしまった → タイトルを正式 §0.41.1-.12 に gh issue edit で修正 + open/close を実装実態に再同期 (#38/#40/#41 を reopen して正式仕様で再実装)**。完了= .1 通常タグ薄緑背景+濃緑文字(#e6f4ea/ok-green, 白文字撤廃)・.2 ショー完全並列化(priority-high background:none+font-weight:400)・.3 期間限定タグ紫#7c3aed(ベスト緑と分離)・.4 タグ表記マップ(プレミアアクセス→DPA/エントリー受付→抽選, TAG_MAP)・.7 降水確率(元から正)・.8 時刻4回以上畳み(最初2+ほかN回)・.9 Liveアクト(該当なし)・.10 chevron化(expand_more/less)・.11 点+凡例(各時間帯の快適度100点満点)・.12 雨雲文末言い切り。**未完2(構造改修・実機要)= #42 .5「この日の概要」見出し+(要確認)説明・#43 .6 スマホ詳細をカード内格納(detail-row→カード内toggle, §0.40.1と同一の大物)**。3緑衝突(通常/期間限定/ベスト)を .1/.3 で解消したのが今回の主眼。**おまけで landed した非正式項目: 順位バッジ(スコア順上位3)・park-tab視認性。harmless。** Vitest 93緑/ESLint 0。教訓再掲: 仕様源(CODE-REQUEST-NEXT)を逐語で読み切ってから gh/実装に着手。スクショ推測と公式仕様を絶対混ぜない。
- **【最重要】git checkout が未コミット Edit を消す事故が頻発 (2026-05-31)** : 検証中に `git checkout <file>` で HEAD に戻すと、同ファイルへの未コミット §0.37 Edit (components badge-long 等) ごと消える。さらに heredoc の cat>> append が @media ラッパー欠落・display 化け・行重複で CSS を破壊した。**鉄則: (1) Edit 済み未コミットファイルに git checkout を使わない、(2) CSS 追加は heredoc append でなく Read→Edit、(3) commit 後は `git show HEAD:file|grep` で実際に入ったか確認、(4) ブラウザ検証は build→preview→実測 (computed display) まで**。
- **§0.33 確認のみ (commit なし)** : cancelHistoryLoader は `import.meta.glob('./cancel-history/*.json')` で任意ファイル名を自動取込。test-dummy.json で実証 (確認後削除)。熱キャン画像 vision → 「2024-07-harmony-heat.json」等を置くだけで getCancelProbability に反映、パターン変更不要。
- **§0.34 iOS PWA バナー + §0.35 ヘルプ拡張 (main 8d2e3f1)** : visitTracker.js (visitCount/pwaBannerDismissedUntil を localStorage)、installBanner.js (iOS Safari [CriOS/FxiOS 除外] ・ 非 PWA [display-mode:standalone / navigator.standalone] ・ 訪問≥2 ・ 未 dismiss の AND で下部 sticky バナー、手順モーダル、もう表示しない=永続/✕=1週間)、main init() で initInstallBanner()。help-content.js (8セクション集約) + help.js を helpHtml+wireHelpTabs のタブ式に拡張。検証は iOS Safari UA エミュ (emulate userAgent) で バナー表示+モーダル+dismiss を実証、デスクトップは非表示。main.js の重複 import `renderScoreLegend as _rsl` (未使用) も除去。**この回は事故防止徹底: 検証→commit を並行バッチにせず 1 ツールずつ、commit 後 `git show HEAD:file|grep` で全項目確認 → 一発成功 (前回までの脱落連鎖なし)**。
- **【最重要・恒久教訓】並行ツールバッチ全キャンセルで Edit が無言脱落 (2026-05-31、§0.28/§0.31/§0.32/§0.36 で連鎖多発)** : 1 つの Bash が permission 拒否/error になると同応答内の後続 Edit/Bash が全て Cancelled → コミットメッセージだけ先行し実変更が入らない。git status クリーンでも HEAD と working が同じ壊れ方なら差分は出ず気づけない。**鉄則: (1) 検証→commit の連鎖では並行バッチを使わず 1 ツールずつ順次、(2) commit 後は必ず `git show HEAD:file | grep` で全変更が入ったか機械確認、(3) old_string 不一致の silent fail も多いので Edit 後に grep 再確認**。[[feedback_pre_commit_race]]
- **ESLint 完全化 (main 5ec7e44)** : scripts/*.mjs が config files 対象外で console/process/fetch を no-undef 誤検出→`npx eslint .` が常に赤→部分実行の癖が §0.28 import 漏れ見逃しの根因 (no-undef 自体は機能、全実行回避が問題)。scripts に globals.node、tests に vitest グローバル手動 (describe/it/expect/vi 等、globals パッケージに vitest 無し)、src に Chart (CDN UMD) globals 追加。全角空白 U+3000 混入除去 ([\s　]→\s で等価)。**以後 `npx eslint .` 0 error、import 漏れは lint で止まる**。
- **§0.32 予報精度ダッシュボード (main 5ec7e44, Phase 3 第2弾)** : 独立ページ /accuracy.html。**vite-plugin-singlefile は複数 input 非対応 (getInlineDynamicImports エラー) → vite.accuracy.config.js を別 config 化し build 2 パス (`vite build && vite build --config vite.accuracy.config.js`、2 パス目 emptyOutDir:false/publicDir:false)**。accuracyLogLoader.js (RMS/bias/timeSeries/notableExamples) + accuracyDashboard.js (Chart.js 時系列 + RMS/バイアス表 + 的中/外し例、7日未満は蓄積中案内)。index フッターに「予報精度を見る」(.app-footer-link)。accuracy-log は空 {} で公開。8日シードで 3 セクション描画を実 DOM 確認。
- **【重大教訓】並行ツールバッチの全キャンセルで Edit が無言脱落 (2026-05-31)** : 1 つの Bash が permission 拒否や error になると同じ応答内の後続 Edit/Bash が全て Cancelled になり、**コミットメッセージだけ先行して実変更が入らない**事故が §0.28/§0.31/§0.32 で連鎖発生 (8b65805 は eslint 変更を謳うも実際は cancelHistoryLoader.js のみ commit)。**対策: (1) 検証→commit の連鎖では並行バッチを使わず 1 ツールずつ順次実行、(2) commit 後に必ず `git show HEAD:file | grep` で意図変更が全部入ったか機械確認、(3) git status クリーン=正常とは限らない (working も HEAD も同じ壊れ方なら差分は出ない)**。[[feedback_pre_commit_race]] [[feedback_parallel_session_isolation]]
- **§0.30 過去風キャン記録 (Stage A=6f85cc3 / Stage B=c0ca607)** : TSUBASA ブログ + X @tdr_syopare_can の月別 PDF (5ヶ月/record計2111) を構造化。Stage A=import-cancel-history.mjs (固定幅 PDF を /M月D日/ でセグメント分割→カラム index でショー割当、status 6 分類) + cancel-history/*.json + show-thresholds.js (ハーモニー ba6/cancel12・Reach pyro8・エレクトリカル cancel10・ビリーヴ ba5/cancel12)。元 PDF/txt は第三者著作物で .gitignore (docs/cancel-history-pdf/、ローカル保持)、抽出 JSON のみ commit。Stage B=windBadge(gust,threshold) をショー別閾値化、evaluateDay が getDaySchedule の high ショーから strictestThreshold 算出。windBadge 境界変更 (< windBa 通常 / [windBa,windCancel) 風バ / [windCancel,+2) 中止リスク高 / >= +2 ほぼ中止)、テスト更新。
- **§0.29 予報精度追跡 (main 8d83af4)** : snapshot-forecast.mjs (jma/openMeteo/wbgt 流用で予報を forecast-snapshots/YYYY-MM-DD.json 保存) + track-accuracy.mjs (アメダス船橋44132 `point/44132/{YYYYMMDD}_{HH}.json` の temp/wind[0] + 環境省 WBGT 実測と比較 → accuracy-log.json 日次追記、実測全 null 日はスキップ)。毎朝 snapshot→翌朝 track 手動運用 (Phase 3 で GA cron)。**fetch モジュールは glob 不使用で Node 直接実行可、ただし showSchedule.js 等 import.meta.glob 使用モジュールは Node 単体不可 (vite 専用)**。
- **§0.27 スマホカード微調整 (main 91d943e)** : 風雨熱を 6 列 grid で 3 等分 (wind wind/rain rain/heat heat)、chevron (col-chev td + .chevron span) を display:none、日付↔スコアピル flex 横並び、開時 ::after「✕ 閉じる」。依頼文の .disney-table/--shadow-card/--primary は未定義 → 実在の .forecast-table/--shadow/--accent にマップ。
- **§0.26.1 レストランショー (main 646f34b)** : kind:'show-restaurant'/time:null は expandShows で1件残し「時刻未定・予約必須」表示 (.priority-restaurant グレー+italic)。showTimes/allShowMarkers は time 無しを filter 除外。bug-hunt で aggregateMetrics の date 伝播漏れ発見・修正 (showWindowHours に date 渡し実スケジュール窓で算定)。§0.26 CSS は並行編集で一度未適用のまま commit → priority-high 効かず再修正 (e359ecf)。CSS 追加後は実 DOM の getComputedStyle/cssRules で適用確認必須。
- **公式スケジュール自動取得は不可と最終結論 (2026-05-30)** : TDR 公式は **Akamai Bot Manager で保護**。curl/WebFetch/headless Chromium は timeout or 空 body。headed (headless:false) でも **calendar ページは HTMLLEN=146 の空シェルを返し TIMES=0/公演名なし**。一度だけ 178KB 取得できたが**再現せず**、反復アクセスで IP/fingerprint がフラグされ homepage すら 146 byte に。**スケジュールは SPA の API 経由でこれも同保護下 → 安定取得は Akamai 回避 (stealth/proxy/sensor replay) が必要で実施しない**。fetch-schedule.mjs はコードとして残すが実運用不可。→ **手動メンテ or FALLBACK 代表時刻更新の二択**。架空プレースホルダ 2026-06.json は削除済み (全日 FALLBACK 表示中)。**教訓 : 1 回の成功で「取れる」と即断しない、反復で再現確認してから報告**。

**Notion DB** : 「ディズニー行く日候補」作成済み (個人 WS、data_source_id=`74c55ded-1c60-454c-833d-e57ee752fb68`、https://www.notion.so/d17f66b8c11b42d5b7e624226e79c6fe )。notion.js に配線済み。ワークスペース直下に作成 ( ユーザー希望ページ「ディズニーおてんき」は claude.ai コネクタ未共有で 404 のため親指定不可 → ID 不変なので手動ドラッグ移動でよい )。artifact 実書込みは Cowork 側 Notion コネクタのアクセスが別途必要。[[reference_notion_workspaces]]

**データ** : 気象庁 + Open-Meteo (m/s)。WBGT は Open-Meteo の気温 ＋ 湿度から簡易計算 ( 環境省 CSV は CORS ヘッダー無しでブラウザ直 fetch 不可、プロキシ経由なら可 )。

**主な仕様逸脱 ( 報告済み )** : テーブルは Grid.js でなく素の HTML。JMA は風速 ･ UV ･ hourly 非構造化のため Open-Meteo 主体。

**残タスク ( ユーザー GUI / 判断要 )** :
- Cowork に `dist/artifact.html` を artifact 登録
- Notion DB「ディズニー行く日候補」を作成 → ID を `src/integrations/notion.js` の NOTION_CONFIG に設定 ( 個人ワークスペース。作成先を聞けば私が作成可 )
- Notion / GCal の Cowork コネクタ ツール ID を notion.js / gcal.js の *_CONFIG.createTool に合わせる ( 仕様書の ID は環境依存 )
- GitHub repo 作成 + push ( 要許可 )

**Phase 2 ( 未着手 )** : OpenWeather 追加 ( workers/openweather-proxy.js デプロイ + CONFIG.openWeatherProxyUrl )、的中追跡 (§3.12)、ソース信頼度補正 (§5.4 土台のみ実装済み)。

パレード時刻は `src/data/showSchedule.js` に固定値、月 1 で目視更新。

- **#26/#27 landed後にスキップ指示 + #19/§0.37.4 重大バグ修正 (2026-05-31, main 5f3a2c1)** : Yuka が #26(cron)/#27(月初通知)/#25(ショー期間) を「急がないのでスキップ」指示。#26/#27 は既に実装push済(daily-snapshot.yml/monthly-schedule.yml, additive で harmless のため残置)、#25 は編集がキャンセルで未landed=指示通り放置。**重大: #19(6eb2032) を ESLint error (diffHtml が return 後のデッドコード=no-unused-vars) のまま push していた手順ミス → 5f3a2c1 で修正。さらに §0.37.4 PCスコア列が commit 7dc54ba 時点から body 側未landed (thead 8列/body 7列=PC列ズレ) で出荷されていたのを発見・復旧 (cell-score-pc td 追加 + カード内 score を .card-score 化, CSS 重複も整理)。** 実測 PC thead8==body8 列ズレ解消・差分マークは前日snapshot無く現状非表示(ガード正常, cron で蓄積後表示, 9テスト担保)。chart.js の不要 eslint-disable 2件も除去。Vitest 107緑/ESLint 0(warning含め0)/build OK。**教訓再掲徹底: commit 前に必ず ESLint exit code を目視 (LINT=0 を確認してから commit)。Edit 失敗時に thead だけ変えて body を変えず出荷する列不一致を二度と起こさない=構造変更後は thead と body の td 数を必ず突合。** 次=Yuka 戻り後 #42/#43(§0.41残)→#17(気象庁pop)→残§0.39(#20-24,#28,#29)。

- **build赤push→即修正 (2026-05-31, main 8d40e29)** : 2adc732 を「diff 二重宣言 parse error」で build/lint 赤のまま push する手順ミス → 8d40e29 で重複ブロック統合し修正。現 HEAD は LINT0/Vitest102緑/build OK ・ L=R 同期。#19予報変更マーク+§0.37.4 PC列ズレ(thead8/body7) も同時に復旧済 (cell-score-pc td 追加)。教訓: commit 前に必ず BUILD/LINT exit code を目視。Edit は既存ブロックを置換せず追加すると二重宣言を生む→ Edit 後に grep -c で宣言数を確認。
